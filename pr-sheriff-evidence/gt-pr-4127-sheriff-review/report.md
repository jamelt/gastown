PR Sheriff Report
Subject: gastownhall/gastown PR #4127 "fix: --force now truly bypasses MR verdict check in polecat nuke (gt-v98fr)" https://github.com/gastownhall/gastown/pull/4127
Mode: confirmatory_review (no code changes to the PR's subject; one unrelated dead-code discovery filed separately as gt-rriy)
Author: athosmartins / Athos Bernardes (furiosa), with Claude Sonnet 4.6 co-authorship on the original commit
PR state: CLOSED (not merged), closed 2026-07-09T16:00:13Z
Superseded by: PR #4446 "fix: guard non-force polecat nuke active MRs" (merged 2026-07-09T15:59:10Z, merge commit b810686e8fe24d6ff325fbd1595700046b52a9e8), authored via bead gt-girs (assignee gastown/polecats/radrat)
Local review branch: polecat/deathclaw/gt-pr-4127-sheriff-review+mszyyzyn
upstream/main checked at: 649b832b7672bc7a2dbef26f5983aba6198b819b
Repo config used: pr-sheriff skill (project-scoped, .claude/skills/pr-sheriff/skill.md); pr-sheriff-config.json and the mol-pr-sheriff-patrol formula referenced by that skill do NOT exist anywhere in this workspace (confirmed by filesystem-wide search) — contributor-tier data was not needed for this disposition since no merge-eligibility judgment was required (subject PR is already closed).
Labels on #4127: kind/bug, priority/p2, status/review-failed (unchanged by this review)

Triage category: superseded_confirmed
Research legs: 19 independent, tool-verified checks performed directly by this bead (exceeds the 15-leg minimum; see Findings 1-11 below, each grounded in a real `gh`/`git`/`bd` command, not narrative restatement).
Pre-decision review legs: 5/5 completed via independent fresh subagents, each given only the compiled facts and told to re-verify at least 2 claims itself with real tool calls before rendering a verdict.
  - Leg 1 (core fix-logic + PR/replacement linkage): CONFIRM
  - Leg 2 (safety-regression-risk, all call sites of the nuke helper): CONFIRM
  - Leg 3 (duplicate/conflicting prior review history): CONFIRM
  - Leg 4 (cleanup-first design quality vs. bandaid check): OBJECT (minor) — found and I independently re-verified a genuine, pre-existing piece of dead code (`nukePolecatFull`, the no-options wrapper, has zero callers) left over from before PR #4446 introduced `nukePolecatOptions`. Not a defect in the disposition itself; filed as follow-up bead gt-rriy per discovered-work protocol rather than fixed in this review-only bead (out of scope: fixing it here would mean editing gastownhall/gastown code from a bead whose acceptance criteria is a PR disposition, not a code change, and cleanup-first still means "smallest correct diff," not "expand scope to grab every adjacent cleanup").
  - Leg 5 (attribution + no-contributor-change-request policy compliance): CONFIRM
  4 of 5 legs confirmed the core disposition outright; the 1 objection was scoped to an out-of-scope, non-blocking discovery and does not change the verdict.
Post-implementation reviews: not_applicable (no code change to the PR's subject was made by this bead; the dead-code discovery is tracked separately in gt-rriy, not implemented here)

Cleanup-first assessment: the replacement (#4446) is genuinely convergent, not a bandaid. Where #4127's own diff bolted a raw `force bool` positional parameter onto `nukePolecatFull` (2 call sites touched), #4446 (a) introduced a `nukePolecatOptions{Force, PurgeClosedEphemerals}` struct instead of growing a positional-bool signature, (b) consolidated two previously-divergent active-MR checks into one shared classifier (`Manager.ActiveMRRemovalBlocker`, used by both `checkNukeActiveMRSafety` and `removeWithOptionsLocked`), and (c) fixed a second, related bug #4127 itself missed: `RemoveWithOptions` was hardcoded to `force=true` regardless of the CLI flag, so worktree-level nuclear removal always bypassed the MR check even when the outer gate correctly blocked. That is a materially better fix than the original PR, not a duplicate-effort or a workaround layer.

Findings

1. Closure verified genuine and current. `gh pr view 4127 --repo gastownhall/gastown` shows CLOSED, not merged, closedAt 2026-07-09T16:00:13Z. Its only substantive comment (author Bella-Giraffety, COLLABORATOR) reads: "Closed as superseded by replacement PR #4446, merged as b810686e8fe24d6ff325fbd1595700046b52a9e8. Original contribution attribution was preserved in the squash merge with Co-authored-by: Athos Bernardes <athosmartins@gmail.com>." The only other comment is an automated codecov test-report (bot, informational, 2 pre-existing flaky tests unrelated to this change). No maintainer or bot ever posted a "please make changes" request to the contributor on this PR.

2. Replacement fix verified merged and correct on current upstream/main. `gh pr view 4446` confirms `state: MERGED`, `mergeCommit.oid: b810686e8fe24d6ff325fbd1595700046b52a9e8` — the exact hash cited in #4127's closing comment. `git merge-base --is-ancestor b810686e8f... upstream/main` (upstream fetched fresh at 649b832b7) returns true. Reading `internal/cmd/polecat.go` on upstream/main directly: `nukePolecatOptions{Force bool; PurgeClosedEphemerals bool}`; `checkNukeActiveMRSafety(checker, name, rig, force)` returns nil (bypasses the block) only `if force || checker == nil`; the manual `gt polecat nuke --force` CLI path (line ~1794) passes `Force: polecatNukeForce`, sourced from the explicit `--force` flag; both the `nukePolecatFull` convenience wrapper and the `gt polecat stale --cleanup` batch path (line ~2178) omit `Force`, leaving it at the Go zero-value `false` — i.e. auto-cleanup still fails closed, exactly as the original bug report demanded and exactly as #4127 itself intended.

3. No safety regression at any call site. All 3 call sites of `nukePolecatFullWithOptions` were enumerated (`git grep`): interactive `polecat nuke` (Force from CLI flag), the no-options `nukePolecatFull` wrapper (Force defaults false — and is itself dead code, see Finding 9), and the `stale --cleanup` batch path (Force defaults false). `checkNukeActiveMRSafety` is invoked from exactly one place, so there is no alternate route that skips the gate. `git log` on the file shows the #4446 merge is the newest commit touching this safety logic — nothing since has weakened it.

4. Attribution independently confirmed in the real commit, not just the GitHub comment. `git show b810686e8fe24d6ff325fbd1595700046b52a9e8 --no-patch --format="%B"` shows the merge commit body itself contains `Co-authored-by: Athos Bernardes <athosmartins@gmail.com>` and `Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>`, plus explicit cross-references to both `https://github.com/gastownhall/gastown/pull/4127` (original) and `.../pull/4446` (replacement). The attribution claim in the closing comment is genuine, not aspirational.

5. No open GitHub issue found that this PR was meant to close or that requires disposition. Searched `gh issue list --search "polecat nuke force"`, `--search "v98fr"` (0 hits), and `--search "MR verdict"` — none reference this specific bug/PR by number or in a way indicating an open tracker exists for it. PR #4127's own body only says "Merge request gt-wisp-7bc for issue gt-v98fr" — `gt-v98fr` is an internal Gas Town bead id, not a GitHub issue, and `bd show gt-v98fr` returns "no issue found" (the bead itself has since aged out of the local Dolt tracker). There is nothing to link, close, or leave open on the GitHub issue tracker for this PR.

6. Full duplicate-review history reconciled — this is the (at least) fifth time this exact PR has been dispatched for Sheriff review, and every prior pass reached the same disposition:
   - `gt-pr-sheriff-4127` (created 2026-06-08, auto-closed stale by reaper 2026-06-29, before any replacement existed)
   - `hq-cv-uu3ic` (2026-06-04, convoy tracker for the above, auto-closed stale)
   - `gt-pr-sheriff-4127-cleanup` (created 2026-06-17, auto-closed stale by reaper 2026-06-29, also before the replacement existed)
   - `hq-9nn` (2026-06-11, github-sheriff CI-failure tracker for #4127's own failing checks — Windows Smoke Test, Test, Lint — auto-closed stale 2026-06-29; moot now since #4127 itself never merged)
   - `gt-girs` (created 2026-07-08, assignee radrat) — this is the bead that actually implemented and merged the replacement, PR #4446, closed 2026-07-09 with reason "Replacement PR #4446 merged as b810686e8f...; original PR #4127 closed as superseded with attribution preserved."
   - `gt-kcb2` (created 2026-07-09, assignee brotherhood) — dispatched concurrently with/just after gt-girs, closed same day: "Superseded while in progress: replacement PR #4446 merged as b810686e8f... and original PR #4127 is closed." Independently corroborates gt-girs rather than conflicting with it.
   No prior bead contradicts this review's verdict, and no other open or in-progress bead currently targets PR #4127 besides this review's own umbrella container.

7. No dangling local git state. `git branch -a` and `git worktree list` show no branch or worktree referencing PR #4127, gt-v98fr, or gt-girs anywhere in this workspace, other than this review's own tracking branch.

8. Test coverage for the underlying safety classifier exists. `internal/polecat/active_mr_test.go` and `internal/cmd/polecat_check_recovery_test.go` both reference `ActiveMRRemovalBlocker`/related check-recovery logic. Codecov's automated comment on #4446 shows all tests passed (0 failed) with 62.86% patch coverage; the 2-line `force`-bool short-circuit inside `checkNukeActiveMRSafety` itself has no dedicated unit test, but the classifier it delegates to is covered, and the branch was independently verified correct by direct code read (Finding 2) rather than by test alone.

9. One genuine, unrelated dead-code finding — filed as follow-up, not fixed here. `nukePolecatFull(polecatName, rigName string, mgr *polecat.Manager, r *rig.Rig) error`, the no-options convenience wrapper around `nukePolecatFullWithOptions`, has zero callers anywhere in the repository (`git grep -n "nukePolecatFull(" upstream/main -- '*.go'` matches only its own definition and the unrelated `...WithOptions` name). It predates PR #4446's introduction of `nukePolecatOptions` and was made redundant by it. Go does not flag unused unexported functions, so it silently persists. This does not affect correctness or the disposition of #4127/#4446 — filed as `gt-rriy` (labelled `discovered-from:gt-pr-4127-sheriff-review`, `cleanup`) per the discovered-work protocol, left for a future cleanup-focused bead rather than expanding this review-only bead's scope.

10. Scope discipline. #4127 and its replacement #4446 are both scoped, targeted bug fixes (correcting an unintentionally-inconsistent safety bypass), not feature/enhancement work — no human approval gate applies. This review made no code changes to gastownhall/gastown; it only records evidence (this report, evidence.json) in the gastown-ops rig and files one out-of-scope discovered-work bead, consistent with "do not implement feature/enhancement work" and "do not broaden scope beyond making this PR clean, safe, and mergeable" (which for an already-closed, already-superseded-and-merged PR means: confirm and record, do not reopen or re-merge).

11. Confirmed via 5 independent fresh-subagent review legs (see "Pre-decision review legs" above) rather than self-assessment alone — each leg was given only the compiled facts, told nothing about the others' conclusions, and required to independently re-run at least 2 verification commands before rendering CONFIRM/OBJECT. 4/5 confirmed outright; the 1 objection (leg 4) surfaced the real-but-out-of-scope dead-code item in Finding 9, which I independently re-verified via `git grep` before filing gt-rriy.

Final verdict: no_action_required (code) — PR #4127 is correctly closed as superseded by a verified, merged, and materially better replacement (#4446). This bead makes no changes to gastownhall/gastown. One follow-up item filed, not executed here: gt-rriy (delete or otherwise resolve the now-dead `nukePolecatFull` wrapper). No GitHub issue requires linking or closing. No contributor-facing action needed or taken.
Merge path allowed: not_applicable (nothing to merge; PR already closed and its fix already shipped)
