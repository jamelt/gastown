PR Sheriff checker output — PR #4066 (gastownhall/gastown)
Bead: gt-pr-4066-sheriff-review
Generated: 2026-08-19

=== 1. Subject PR state ===

$ gh pr view 4066 --repo gastownhall/gastown --json number,title,state,mergedAt,labels
number: 4066
title: fix(formula): resolve extends/compose in prime + inject issue= sling var
state: OPEN
mergedAt: null
labels: kind/bug, priority/p2, status/review-failed

$ gh pr view 4066 --repo gastownhall/gastown --json statusCheckRollup
Test: FAILURE (2026-05-20)
Lint: SUCCESS
Integration Tests: SUCCESS
Windows Smoke Test: SUCCESS
(base 445fd8fd, ~3 months stale as of this review; never re-run since 2026-06-17)

=== 2. formula.Resolve() confirmed unused in rendering path (bug 1, pre-fix) ===

$ git fetch upstream main && git rev-parse upstream/main
649b832b7672bc7a2dbef26f5983aba6198b819b

$ git grep -n "formula.Resolve(" upstream/main -- internal/cmd/
(no output — zero callers)

$ git show upstream/main:internal/formula/parser.go | grep -n "^func Resolve"
508:func Resolve(formula *Formula, searchPaths []string) (*Formula, error) {

=== 3. sling.go / sling_dispatch.go already fixed independently (bug 2a/2b moot) ===

$ git show upstream/main:internal/cmd/sling.go | sed -n '975,982p'
        attachedMoleculeID = result.WispRootID
        if len(result.FormulaVars) > 0 {
                varsForAttachment = append([]string(nil), result.FormulaVars...)
                formulaVarsForAttachment = strings.Join(result.FormulaVars, "\n")
        }

$ git show upstream/main:internal/cmd/sling_helpers.go | grep -n "func formulaVarsForBead" -A6
1076:func formulaVarsForBead(formulaName, beadID, title string, extraVars []string) []string {
1077:    formulaVars := []string{
1078:        fmt.Sprintf("feature=%s", title),
1079:        fmt.Sprintf("issue=%s", beadID),
1080:    }
(both direct sling and sling_dispatch.go's executeSling route through this — issue= already injected)

=== 4. sling_formula.go standalone-sling gap confirmed (bug 2c, pre-fix) ===

$ git show upstream/main:internal/cmd/sling_formula.go | sed -n '525,532p'
        fieldUpdates := beadFieldUpdates{
                Dispatcher:      actor,
                Args:            slingArgs,
                Vars:            append([]string(nil), slingVars...),
                AttachedFormula: formulaName,
                Mode:            &mode,
                FormulaVars:     strings.Join(slingVars, "\n"),
        }
(no issue= prepended, despite wispRootID being known at this point)

=== 5. Prior replacement PR #4445 — stale and attribution defect ===

$ gh pr view 4445 --repo gastownhall/gastown --json mergeable,mergeStateStatus,state
mergeable: CONFLICTING
mergeStateStatus: DIRTY
state: OPEN

$ gh api repos/gastownhall/gastown/pulls/4445/commits --jq '.[0].commit.message'
WIP: checkpoint (auto)
(no body, no Co-authored-by trailer for flipch, despite PR body claiming attribution preserved)

=== 6. Fresh fix branch — build/vet/test (local, this session) ===

Branch: polecat/brotherhood/gt-pr-4066-fix+275114, based on upstream/main @ 649b832b7

$ CGO_ENABLED=0 go build ./...
(clean, no output)

$ CGO_ENABLED=0 go vet ./internal/cmd/... ./internal/formula/...
(clean, no output)

$ CGO_ENABLED=0 go test ./internal/cmd/... ./internal/formula/...
ok  	github.com/steveyegge/gastown/internal/cmd	40.585s
ok  	github.com/steveyegge/gastown/internal/formula	(cached)

=== 7. Regression test evidence: fails pre-fix, passes post-fix ===

$ git stash push -- internal/cmd/prime_molecule.go internal/cmd/sling_formula.go
$ go test ./internal/cmd/... -run 'TestRenderFormulaStepsFull_ResolvesExtendsAndCompose|TestRunSlingFormulaPersistsVarContext' -v
--- FAIL: TestRenderFormulaStepsFull_ResolvesExtendsAndCompose (0.00s)
    prime_test.go:87: rendered mol-polecat-work-monorepo-tdd missing "Load context and verify assignment" (extends/compose not resolved)
--- FAIL: TestRunSlingFormulaPersistsVarContext (0.01s)
    sling_test.go:2230: issue= var (wisp root ID) missing from persisted standalone formula description

$ git stash pop
$ go test ./internal/cmd/... -run 'TestRenderFormulaStepsFull_ResolvesExtendsAndCompose|TestRunSlingFormulaPersistsVarContext' -v
--- PASS: TestRenderFormulaStepsFull_ResolvesExtendsAndCompose (0.00s)
--- PASS: TestRunSlingFormulaPersistsVarContext (0.01s)

=== 8. Branch hygiene gate (before opening replacement PR) ===

$ gt pr-sheriff-check --merge-gate --base upstream/main --json
{"base":"upstream/main","ahead":1,"behind":0,"severity":"clean","reasons":[],"merge_gate":true,"merge_path_allowed":true}

=== 9. Replacement PR opened ===

$ gh pr create --repo gastownhall/gastown --base main --head jamelt:polecat/brotherhood/gt-pr-4066-fix+275114 ...
https://github.com/gastownhall/gastown/pull/4725

$ gh pr view 4725 --repo gastownhall/gastown --json additions,deletions,changedFiles,headRefOid,mergeable,mergeStateStatus,state
additions: 51, deletions: 2, changedFiles: 4
headRefOid: 3e04c43f43b57a68dab719afab7c8e1112eff353
mergeable: MERGEABLE
mergeStateStatus: UNSTABLE
state: OPEN

$ gh pr checks 4725 --repo gastownhall/gastown
add-triage-label	pass	4s
[code]smith	skipping	0
(main CI workflows — Test/Lint/Integration Tests/Windows Smoke Test — show action_required:
first-time/fork-PR workflow runs on gastownhall/gastown require a maintainer to approve
before they execute. Not a failure; gated on maintainer action, independently confirmed
by an independent post-implementation review agent that cloned PR #4725's head and ran
go build/vet/test directly — all green locally.)

=== 10. Independent commit-trailer check (attribution) ===

$ gh pr view 4725 --repo gastownhall/gastown --json commits --jq '.commits[0].messageBody' | grep -i co-authored
Co-authored-by: flipch <10780503+flipch@users.noreply.github.com>
Co-authored-by: Claude Sonnet 5 <noreply@anthropic.com>

=== 11. Stale replacement #4445 disposition ===

$ gh pr comment 4445 --repo gastownhall/gastown --body "Superseded by #4725 ..."
https://github.com/gastownhall/gastown/pull/4445#issuecomment-5341502446

$ gh pr close 4445 --repo gastownhall/gastown
API call failed: GraphQL: jamelt does not have the correct permissions to execute
`ClosePullRequest` (closePullRequest)
(no write/close access to gastownhall/gastown from this fork identity; recommendation
left as a PR comment for a maintainer to act on, per policy — Sheriff does not force
actions beyond its actual GitHub permissions)

=== 12. Related-issue disposition ===

Issue #3322 ("gt prime showFormulaStepsFull only reads embedded formulas"): confirmed via
`git show upstream/main:internal/formula/embed.go` that ResolveFormulaContent already
implements rig->town->embedded 3-tier resolution — the issue's reported repro (embedded-only
lookup, zero disk fallback) is already fixed on current main, independently of this PR chain.
Distinct root cause from #4066/#4725 (disk-fallback tier vs extends/compose resolution).
No action taken on #3322 by this review; noted as already-resolved/unrelated.

No other open GitHub issue references PR #4066 by number in body text (checked #4066's own
body, #4445's body, and an issue search sweep).
